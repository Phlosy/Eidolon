import { GitSettingsSection } from "../../components/git/git-settings-section";

/** Git 集成：内置 Gitea 或外部 GitLab / GitHub Enterprise 等。 */
export function GitSection() {
  return <GitSettingsSection />;
}
