export const designTheme = {
  radius: { panel: "var(--radius-panel)", card: "var(--radius-card)" },
  shadow: { panel: "var(--shadow-panel)", floating: "var(--shadow-floating)" },
  glow: { primary: "var(--glow-primary)", success: "var(--glow-success)" },
} as const;
